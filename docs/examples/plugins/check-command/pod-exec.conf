object CheckCommand "pod-exec" {
  import "plugin-check-command"
  command = [ PluginDir + "/hyperalert", "check_pod_exec"]

  arguments = {
	"--iv-checkInterval" = "$service.check_interval$"
	"--container" = "$container$"
	"--cmd" = "$cmd$"
	"--argv" = "$argv$"
	"--iv-host" = "$host.name$"
	"--v" = "$host.vars.verbosity$"
  }
}